from fastapi import APIRouter, UploadFile, File
from services.aws_service import upload_file

app = 
router = APIRouter()

@router.post("/upload")
async def upload_image(file: UploadFile = File(...)):

    result = upload_file(file.file)

    return result
